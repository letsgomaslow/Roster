# Copy to `.env.local` at the repo root (gitignored) after `pnpm exec convex login`.
# Replace the deployment line if the dashboard shows a different type/slug (dev vs prod).
#
# CONVEX_DEPLOYMENT format: prod:deployment-name or dev:deployment-name (see Convex dashboard).
# Your cloud URL: https://marvelous-starling-320.convex.cloud
CONVEX_DEPLOYMENT=prod:marvelous-starling-320
CONVEX_URL=https://marvelous-starling-320.convex.cloud
